# Database configuration for POS Java Application
# Copy this file to .env and configure your database settings

# ========== LOCAL DATABASE (Recommended for Development) ==========
# Local PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pos_db
DB_USER=pos_user
DB_PASSWORD=pos_password

# ========== ONLINE DATABASE (Optional) ==========
# If you have a cloud database, uncomment and configure:
# POS_DB_URL=jdbc:postgresql://your-cloud-host/your-database?sslmode=require
# POS_DB_USER=your_username
# POS_DB_PASSWORD=your_password

# ========== SETUP INSTRUCTIONS ==========
# 1. Copy this file to .env
# 2. Update the database credentials above
# 3. Make sure PostgreSQL is installed and running
# 4. Create the database and user:
#    psql -U postgres
#    CREATE DATABASE pos_db;
#    CREATE USER pos_user WITH PASSWORD 'pos_password';
#    GRANT ALL PRIVILEGES ON DATABASE pos_db TO pos_user;
#    ALTER DATABASE pos_db OWNER TO pos_user;

# ========== NOTES ==========
# - The application will automatically create tables on first run
# - Default credentials work with setup-environment.bat
# - Keep this file secure and never commit .env to version control